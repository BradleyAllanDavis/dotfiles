export platform=$(uname)
if [[ "$platform" == 'Linux' ]]; then
  # Set key repeat delay and rate
  [ -f `which xset` ] && `which xset` r rate 150 100
fi
















































# vim: fdm=marker:fdl=0 ai ts=2 sw=2 et sts=2 ft=sh
